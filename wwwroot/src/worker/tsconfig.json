{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        "lib": ["WebWorker"],
        "skipLibCheck": true // Because @types/node breaks WW types
    },
    "references": [
        { "path": "../raytracer" }
    ]
}
